# Evaluation Metrics Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement `src/clinical_codes/evaluation/metrics.py` — Pydantic output models, three pure metric functions, and a `compute_metrics()` aggregator that produces a fully populated `MetricsSummary` from `list[RunResult]` + `list[GoldQuery]`.

**Architecture:** Three private functions (`_system_f1`, `_recall_at_k`, `_must_include_hit_rate`) handle per-query computation. A private `_aggregate_query_metrics` helper folds a list of `QueryMetrics` into aggregate scalars. `compute_metrics()` joins results to gold, calls the per-query functions, groups by query type, and returns a single `MetricsSummary`. All models live in `metrics.py` (not `schema.py`) so reporter.py can import both models and computation from one place.

**Tech Stack:** Pydantic v2, Python 3.11+, pytest, uv

---

## File map

| File | Action |
|---|---|
| `src/clinical_codes/evaluation/metrics.py` | Create — models + functions |
| `tests/evaluation/test_metrics.py` | Create — unit tests |

---

## Task 1: Data models scaffold

**Files:**
- Create: `src/clinical_codes/evaluation/metrics.py`
- Create: `tests/evaluation/test_metrics.py`

- [ ] **Step 1: Write the failing import test**

Create `tests/evaluation/test_metrics.py`:

```python
from clinical_codes.evaluation.metrics import MetricsSummary, QueryMetrics, QueryTypeMetrics
from clinical_codes.schemas import SystemName


def test_query_metrics_instantiates() -> None:
    qm = QueryMetrics(
        query_id="q001",
        query="diabetes",
        query_type="simple",
        system_precision=1.0,
        system_recall=1.0,
        system_f1=1.0,
        recall_at_3=1.0,
        must_include_hit_rate=None,
        iterations=1,
        api_calls=1,
        latency_s=0.5,
        error=None,
    )
    assert qm.query_id == "q001"
    assert qm.must_include_hit_rate is None


def test_metrics_summary_instantiates() -> None:
    summary = MetricsSummary(
        n_total=1,
        n_errors=0,
        system_selection_f1=1.0,
        top3_recall=1.0,
        must_include_hit_rate=1.0,
        mean_iterations=1.0,
        mean_api_calls=1.0,
        by_type={},
        per_query=[],
    )
    assert summary.n_total == 1
```

- [ ] **Step 2: Run to confirm failure**

```
uv run pytest tests/evaluation/test_metrics.py -v
```

Expected: `ImportError: cannot import name 'MetricsSummary' from 'clinical_codes.evaluation.metrics'` (module does not exist yet).

- [ ] **Step 3: Create `metrics.py` with the three data models**

Create `src/clinical_codes/evaluation/metrics.py`:

```python
from pydantic import BaseModel


class QueryMetrics(BaseModel):
    query_id: str
    query: str
    query_type: str
    system_precision: float
    system_recall: float
    system_f1: float
    recall_at_3: float | None
    must_include_hit_rate: float | None
    iterations: int
    api_calls: int
    latency_s: float
    error: str | None


class QueryTypeMetrics(BaseModel):
    query_type: str
    n: int
    system_selection_f1: float
    top3_recall: float | None
    must_include_hit_rate: float | None
    mean_iterations: float
    mean_api_calls: float


class MetricsSummary(BaseModel):
    n_total: int
    n_errors: int
    system_selection_f1: float
    top3_recall: float
    must_include_hit_rate: float
    mean_iterations: float
    mean_api_calls: float
    by_type: dict[str, QueryTypeMetrics]
    per_query: list[QueryMetrics]
```

- [ ] **Step 4: Run to confirm pass**

```
uv run pytest tests/evaluation/test_metrics.py -v
```

Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add src/clinical_codes/evaluation/metrics.py tests/evaluation/test_metrics.py
git commit -m "feat: add evaluation metrics data models"
```

---

## Task 2: `_system_f1`

**Files:**
- Modify: `tests/evaluation/test_metrics.py` — add 4 tests
- Modify: `src/clinical_codes/evaluation/metrics.py` — add function

- [ ] **Step 1: Write the failing tests**

Append to `tests/evaluation/test_metrics.py`:

```python
# ── _system_f1 ────────────────────────────────────────────────────────────────

from clinical_codes.evaluation.metrics import _system_f1


def test_system_f1_perfect_match() -> None:
    p, r, f1 = _system_f1([SystemName.ICD10CM], [SystemName.ICD10CM])
    assert p == 1.0
    assert r == 1.0
    assert f1 == 1.0


def test_system_f1_miss_query_both_empty() -> None:
    p, r, f1 = _system_f1([], [])
    assert f1 == 1.0


def test_system_f1_hallucinated_systems() -> None:
    # predicted non-empty, expected empty → F1 = 0
    p, r, f1 = _system_f1([SystemName.ICD10CM], [])
    assert f1 == 0.0


def test_system_f1_partial_overlap() -> None:
    p, r, f1 = _system_f1(
        [SystemName.ICD10CM, SystemName.LOINC],
        [SystemName.ICD10CM, SystemName.RXNORM],
    )
    assert p == 0.5
    assert r == 0.5
    assert abs(f1 - 0.5) < 1e-9
```

- [ ] **Step 2: Run to confirm failure**

```
uv run pytest tests/evaluation/test_metrics.py::test_system_f1_perfect_match -v
```

Expected: `ImportError: cannot import name '_system_f1'`.

- [ ] **Step 3: Implement `_system_f1`**

Add `from clinical_codes.schemas import SystemName` to the imports at the top of `src/clinical_codes/evaluation/metrics.py`, then add the function after the model definitions:

```python
def _system_f1(
    predicted: list[SystemName],
    expected: list[SystemName],
) -> tuple[float, float, float]:
    tp = len(set(predicted) & set(expected))
    precision = tp / len(predicted) if predicted else 1.0
    recall = tp / len(expected) if expected else 1.0
    f1 = 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0.0
    return precision, recall, f1
```

- [ ] **Step 4: Run to confirm pass**

```
uv run pytest tests/evaluation/test_metrics.py -v
```

Expected: 6 passed.

- [ ] **Step 5: Commit**

```bash
git add src/clinical_codes/evaluation/metrics.py tests/evaluation/test_metrics.py
git commit -m "feat: add _system_f1 metric function"
```

---

## Task 3: `_recall_at_k`

**Files:**
- Modify: `tests/evaluation/test_metrics.py` — add 4 tests
- Modify: `src/clinical_codes/evaluation/metrics.py` — add function

- [ ] **Step 1: Write the failing tests**

Append to `tests/evaluation/test_metrics.py`:

```python
# ── _recall_at_k ──────────────────────────────────────────────────────────────

from clinical_codes.evaluation.metrics import _recall_at_k


def test_recall_at_k_hit_in_top3() -> None:
    result = _recall_at_k(
        {SystemName.ICD10CM: ["E11.9", "E10.9", "E11.65"]},
        {SystemName.ICD10CM: ["E11.9"]},
    )
    assert result == 1.0


def test_recall_at_k_miss_beyond_k() -> None:
    # E11.9 at position index 3 (4th result), outside top-3
    result = _recall_at_k(
        {SystemName.ICD10CM: ["E10.9", "E11.65", "J45.9", "E11.9"]},
        {SystemName.ICD10CM: ["E11.9"]},
        k=3,
    )
    assert result == 0.0


def test_recall_at_k_miss_query_returns_none() -> None:
    result = _recall_at_k({}, {})
    assert result is None


def test_recall_at_k_multi_system_partial() -> None:
    # expected: E11.9 (ICD10CM) + 860975, 6809 (RXNORM)
    # predicted: E11.9 hit; 860975 miss (not in list); 6809 hit
    result = _recall_at_k(
        {SystemName.ICD10CM: ["E11.9"], SystemName.RXNORM: ["6809", "862001"]},
        {SystemName.ICD10CM: ["E11.9"], SystemName.RXNORM: ["860975", "6809"]},
        k=3,
    )
    assert result is not None
    assert abs(result - 2 / 3) < 1e-9
```

- [ ] **Step 2: Run to confirm failure**

```
uv run pytest tests/evaluation/test_metrics.py::test_recall_at_k_hit_in_top3 -v
```

Expected: `ImportError: cannot import name '_recall_at_k'`.

- [ ] **Step 3: Implement `_recall_at_k`**

Add to `src/clinical_codes/evaluation/metrics.py`:

```python
def _recall_at_k(
    predicted_codes: dict[SystemName, list[str]],
    expected_codes: dict[SystemName, list[str]],
    k: int = 3,
) -> float | None:
    if not expected_codes:
        return None
    total = 0
    hits = 0
    for system, codes in expected_codes.items():
        top_k = predicted_codes.get(system, [])[:k]
        for code in codes:
            total += 1
            if code in top_k:
                hits += 1
    return hits / total if total else None
```

- [ ] **Step 4: Run to confirm pass**

```
uv run pytest tests/evaluation/test_metrics.py -v
```

Expected: 10 passed.

- [ ] **Step 5: Commit**

```bash
git add src/clinical_codes/evaluation/metrics.py tests/evaluation/test_metrics.py
git commit -m "feat: add _recall_at_k metric function"
```

---

## Task 4: `_must_include_hit_rate`

**Files:**
- Modify: `tests/evaluation/test_metrics.py` — add 4 tests
- Modify: `src/clinical_codes/evaluation/metrics.py` — add function

- [ ] **Step 1: Write the failing tests**

Append to `tests/evaluation/test_metrics.py`:

```python
# ── _must_include_hit_rate ────────────────────────────────────────────────────

from clinical_codes.evaluation.metrics import _must_include_hit_rate


def test_must_include_hit() -> None:
    result = _must_include_hit_rate(
        {SystemName.ICD10CM: ["E11.9", "E10.9"]},
        ["E11.9"],
    )
    assert result == 1.0


def test_must_include_miss() -> None:
    result = _must_include_hit_rate(
        {SystemName.ICD10CM: ["E10.9"]},
        ["E11.9"],
    )
    assert result == 0.0


def test_must_include_empty_returns_none() -> None:
    result = _must_include_hit_rate(
        {SystemName.ICD10CM: ["E11.9"]},
        [],
    )
    assert result is None


def test_must_include_partial() -> None:
    # E11.9 present; I10 not present → 1/2
    result = _must_include_hit_rate(
        {SystemName.ICD10CM: ["E11.9"]},
        ["E11.9", "I10"],
    )
    assert result == 0.5
```

- [ ] **Step 2: Run to confirm failure**

```
uv run pytest tests/evaluation/test_metrics.py::test_must_include_hit -v
```

Expected: `ImportError: cannot import name '_must_include_hit_rate'`.

- [ ] **Step 3: Implement `_must_include_hit_rate`**

Add to `src/clinical_codes/evaluation/metrics.py`:

```python
def _must_include_hit_rate(
    predicted_codes: dict[SystemName, list[str]],
    must_include: list[str],
) -> float | None:
    if not must_include:
        return None
    all_predicted = {code for codes in predicted_codes.values() for code in codes}
    hits = sum(1 for code in must_include if code in all_predicted)
    return hits / len(must_include)
```

- [ ] **Step 4: Run to confirm pass**

```
uv run pytest tests/evaluation/test_metrics.py -v
```

Expected: 14 passed.

- [ ] **Step 5: Commit**

```bash
git add src/clinical_codes/evaluation/metrics.py tests/evaluation/test_metrics.py
git commit -m "feat: add _must_include_hit_rate metric function"
```

---

## Task 5: `compute_metrics`

**Files:**
- Modify: `tests/evaluation/test_metrics.py` — add 3 tests
- Modify: `src/clinical_codes/evaluation/metrics.py` — add `_aggregate_query_metrics` + `compute_metrics`

- [ ] **Step 1: Write the failing tests**

Append to `tests/evaluation/test_metrics.py`:

```python
# ── compute_metrics ───────────────────────────────────────────────────────────

from clinical_codes.evaluation.metrics import compute_metrics
from clinical_codes.evaluation.schema import GoldQuery, RunResult


def _gold(
    id: str,
    query_type: str,
    expected_systems: list[SystemName],
    expected_codes: dict[SystemName, list[str]],
    must_include: list[str],
) -> GoldQuery:
    return GoldQuery(
        id=id,
        query=f"query_{id}",
        query_type=query_type,
        expected_systems=expected_systems,
        expected_codes=expected_codes,
        must_include=must_include,
        must_not_include=[],
    )


def _result(
    query_id: str,
    query_type: str,
    predicted_systems: list[SystemName],
    predicted_codes: dict[SystemName, list[str]],
    error: str | None = None,
) -> RunResult:
    return RunResult(
        query_id=query_id,
        query=f"query_{query_id}",
        query_type=query_type,
        predicted_systems=predicted_systems,
        predicted_codes=predicted_codes,
        iterations=1,
        api_calls=len(predicted_systems) or 1,
        latency_s=0.5,
        error=error,
        summary="",
    )


def test_compute_metrics_happy_path() -> None:
    gold = [
        _gold("q1", "simple", [SystemName.ICD10CM], {SystemName.ICD10CM: ["E11.9"]}, ["E11.9"]),
        _gold("q2", "simple", [SystemName.ICD10CM], {SystemName.ICD10CM: ["I10"]}, []),
    ]
    results = [
        _result("q1", "simple", [SystemName.ICD10CM], {SystemName.ICD10CM: ["E11.9", "E10.9"]}),
        _result("q2", "simple", [], {}, error="timeout"),
    ]
    summary = compute_metrics(results, gold)

    assert summary.n_total == 2
    assert summary.n_errors == 1
    assert summary.system_selection_f1 < 1.0  # error on q2 drags F1 below 1.0
    assert "simple" in summary.by_type
    assert summary.by_type["simple"].n == 2
    assert len(summary.per_query) == 2


def test_compute_metrics_miss_query_excluded_from_recall() -> None:
    gold = [_gold("q1", "miss", [], {}, [])]
    results = [_result("q1", "miss", [], {})]
    summary = compute_metrics(results, gold)

    assert summary.system_selection_f1 == 1.0
    assert summary.per_query[0].recall_at_3 is None
    assert summary.by_type["miss"].top3_recall is None


def test_compute_metrics_by_type_keys() -> None:
    gold = [
        _gold("q1", "simple", [SystemName.ICD10CM], {SystemName.ICD10CM: ["E11.9"]}, []),
        _gold("q2", "multi_system", [SystemName.ICD10CM, SystemName.LOINC], {}, []),
    ]
    results = [
        _result("q1", "simple", [SystemName.ICD10CM], {SystemName.ICD10CM: ["E11.9"]}),
        _result("q2", "multi_system", [SystemName.ICD10CM, SystemName.LOINC], {}),
    ]
    summary = compute_metrics(results, gold)

    assert set(summary.by_type.keys()) == {"simple", "multi_system"}
```

- [ ] **Step 2: Run to confirm failure**

```
uv run pytest tests/evaluation/test_metrics.py::test_compute_metrics_happy_path -v
```

Expected: `ImportError: cannot import name 'compute_metrics'`.

- [ ] **Step 3: Implement `_aggregate_query_metrics` and `compute_metrics`**

Add these two functions and the necessary imports to `src/clinical_codes/evaluation/metrics.py`. The full file at this point:

```python
from pydantic import BaseModel

from clinical_codes.evaluation.schema import GoldQuery, RunResult
from clinical_codes.schemas import SystemName


class QueryMetrics(BaseModel):
    query_id: str
    query: str
    query_type: str
    system_precision: float
    system_recall: float
    system_f1: float
    recall_at_3: float | None
    must_include_hit_rate: float | None
    iterations: int
    api_calls: int
    latency_s: float
    error: str | None


class QueryTypeMetrics(BaseModel):
    query_type: str
    n: int
    system_selection_f1: float
    top3_recall: float | None
    must_include_hit_rate: float | None
    mean_iterations: float
    mean_api_calls: float


class MetricsSummary(BaseModel):
    n_total: int
    n_errors: int
    system_selection_f1: float
    top3_recall: float
    must_include_hit_rate: float
    mean_iterations: float
    mean_api_calls: float
    by_type: dict[str, QueryTypeMetrics]
    per_query: list[QueryMetrics]


def _system_f1(
    predicted: list[SystemName],
    expected: list[SystemName],
) -> tuple[float, float, float]:
    tp = len(set(predicted) & set(expected))
    precision = tp / len(predicted) if predicted else 1.0
    recall = tp / len(expected) if expected else 1.0
    f1 = 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0.0
    return precision, recall, f1


def _recall_at_k(
    predicted_codes: dict[SystemName, list[str]],
    expected_codes: dict[SystemName, list[str]],
    k: int = 3,
) -> float | None:
    if not expected_codes:
        return None
    total = 0
    hits = 0
    for system, codes in expected_codes.items():
        top_k = predicted_codes.get(system, [])[:k]
        for code in codes:
            total += 1
            if code in top_k:
                hits += 1
    return hits / total if total else None


def _must_include_hit_rate(
    predicted_codes: dict[SystemName, list[str]],
    must_include: list[str],
) -> float | None:
    if not must_include:
        return None
    all_predicted = {code for codes in predicted_codes.values() for code in codes}
    hits = sum(1 for code in must_include if code in all_predicted)
    return hits / len(must_include)


def _aggregate_query_metrics(qms: list[QueryMetrics]) -> dict:
    n = len(qms)
    recall_vals = [qm.recall_at_3 for qm in qms if qm.recall_at_3 is not None]
    mi_vals = [qm.must_include_hit_rate for qm in qms if qm.must_include_hit_rate is not None]
    return {
        "n": n,
        "system_selection_f1": sum(qm.system_f1 for qm in qms) / n if n else 0.0,
        "top3_recall": sum(recall_vals) / len(recall_vals) if recall_vals else None,
        "must_include_hit_rate": sum(mi_vals) / len(mi_vals) if mi_vals else None,
        "mean_iterations": sum(qm.iterations for qm in qms) / n if n else 0.0,
        "mean_api_calls": sum(qm.api_calls for qm in qms) / n if n else 0.0,
    }


def compute_metrics(
    results: list[RunResult],
    gold: list[GoldQuery],
) -> MetricsSummary:
    gold_by_id = {gq.id: gq for gq in gold}
    per_query: list[QueryMetrics] = []
    for result in results:
        gq = gold_by_id[result.query_id]
        p, r, f1 = _system_f1(result.predicted_systems, gq.expected_systems)
        per_query.append(QueryMetrics(
            query_id=result.query_id,
            query=result.query,
            query_type=result.query_type,
            system_precision=p,
            system_recall=r,
            system_f1=f1,
            recall_at_3=_recall_at_k(result.predicted_codes, gq.expected_codes),
            must_include_hit_rate=_must_include_hit_rate(result.predicted_codes, gq.must_include),
            iterations=result.iterations,
            api_calls=result.api_calls,
            latency_s=result.latency_s,
            error=result.error,
        ))

    by_type_groups: dict[str, list[QueryMetrics]] = {}
    for qm in per_query:
        by_type_groups.setdefault(qm.query_type, []).append(qm)

    by_type = {
        qt: QueryTypeMetrics(query_type=qt, **_aggregate_query_metrics(qms))
        for qt, qms in by_type_groups.items()
    }

    overall = _aggregate_query_metrics(per_query)
    return MetricsSummary(
        n_total=len(results),
        n_errors=sum(1 for r in results if r.error is not None),
        system_selection_f1=overall["system_selection_f1"],
        top3_recall=overall["top3_recall"] or 0.0,
        must_include_hit_rate=overall["must_include_hit_rate"] or 0.0,
        mean_iterations=overall["mean_iterations"],
        mean_api_calls=overall["mean_api_calls"],
        by_type=by_type,
        per_query=per_query,
    )
```

- [ ] **Step 4: Run to confirm pass**

```
uv run pytest tests/evaluation/test_metrics.py -v
```

Expected: 17 passed.

- [ ] **Step 5: Run the full test suite to confirm no regressions**

```
uv run pytest -v
```

Expected: all previously passing tests still pass (68 passed + 17 new = 85 passed), 1 integration test skipped by default.

- [ ] **Step 6: Commit**

```bash
git add src/clinical_codes/evaluation/metrics.py tests/evaluation/test_metrics.py
git commit -m "feat: add compute_metrics aggregator"
```
